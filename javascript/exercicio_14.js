let palavra;

for( var i = palavra.length; i > 0; i-- )
{
    if( palavra[i] = palavra.charAt(palavra.length)-1 )
    {
        document.write('the Word is palavrae.');
    }else{
        document.write('the Word is not palavrae!');
    }
}
